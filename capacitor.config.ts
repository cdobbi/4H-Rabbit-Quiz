import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.cdobbi.rabbithusbandryquiz',
  appName: 'Rabbit Husbandry Quiz',
  webDir: 'www'
};

export default config;
